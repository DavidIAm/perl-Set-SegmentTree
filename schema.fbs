namespace IntervalTree;

file_identifier "RTRE";

struct node {
  min_time : int;
  min_node : int;
  max_time : int;
  max_node : int;
  key : string;
}

table ValueLookup {
  root : node;
  nodes: [node];
  created : int;
}

root_type ValueLookup;
